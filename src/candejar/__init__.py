"""candejar -- a CANDE preprocessor core.

Reads, validates, and writes CANDE ``.cid`` input files.  This package is the
open core: the fixed-column codec, the domain model, and the validation rules.
Mesh generation, solver integration, and the full application live in the
separate ``candejar_pro`` distribution (see ``docs/REDESIGN-PROPOSAL.md`` 4.6).
"""

__version__ = "0.1.0.dev0"
